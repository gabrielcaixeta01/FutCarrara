# PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o Fut Carrara instalável como PWA e 100% funcional offline, sem dependências novas.

**Architecture:** Manifest estático + ícones commitados em `public/`; service worker gerado por script postbuild que varre `./out` e injeta a lista de precache num template escrito à mão; registro via componente client montado no layout. Spec: `docs/superpowers/specs/2026-07-14-pwa-design.md`.

**Tech Stack:** Next.js 15 (`output: 'export'`), Node puro (fs/path/crypto/child_process), Vitest.

## Global Constraints

- **Zero dependências novas** no `package.json` (regra do CLAUDE.md).
- TypeScript strict, sem `any`.
- Componentes: `PascalCase.tsx`, export nomeado. Texto de UI em pt-BR, código em inglês.
- Hospedagem: raiz do domínio → paths absolutos (`/manifest.json`, `/sw.js`).
- Atualização passiva: **sem** `skipWaiting`, sem UI de update.
- Cor do app: `#0b1a12` (já usada no `viewport.themeColor` do layout).
- Build de referência: `out/` contém `index.html`, `elenco.html`, `sorteio.html`, `resultado.html`, `404.html`, payloads RSC `*.txt` (`index.txt`, `elenco.txt`, …) e `_next/static/**`. Os `.txt` são buscados pelo client com query `?_rsc=...` — o SW precisa de `ignoreSearch` no match.
- Rasterizador SVG→PNG: `rsvg-convert` (disponível em `~/miniconda3/bin`), fallback `sips` (macOS). Detectado em runtime pelo script; nada entra no `package.json`.

---

### Task 1: Ícones do PWA

**Files:**
- Create: `scripts/generate-icons.mjs`
- Create (gerados e commitados): `public/icon-192.png`, `public/icon-512.png`, `public/icon-maskable-512.png`, `public/apple-touch-icon.png`

**Interfaces:**
- Consumes: nada.
- Produces: os 4 PNGs em `public/` com esses nomes exatos — Task 2 (manifest e metadata) referencia `/icon-192.png`, `/icon-512.png`, `/icon-maskable-512.png`, `/apple-touch-icon.png`.

- [ ] **Step 1: Escrever `scripts/generate-icons.mjs`**

```js
// Gera os ícones do PWA em public/ a partir de um SVG desenhado aqui.
// Uso: node scripts/generate-icons.mjs
// Requer rsvg-convert ou sips (macOS) no PATH — nada de dependência npm.
import { execFileSync } from 'node:child_process';
import { mkdtempSync, rmSync, writeFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

const BG = '#0b1a12';
const LINE = '#1e4030';
const FG = '#eaf6ec';

// scale < 1 encolhe o conteúdo para a safe zone de ícones maskable.
function iconSvg(scale) {
  const r = Math.round(148 * scale);
  const fontSize = Math.round(190 * scale);
  const baseline = Math.round(256 + 66 * scale);
  return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" fill="${BG}"/>
  <line x1="0" y1="256" x2="512" y2="256" stroke="${LINE}" stroke-width="14"/>
  <circle cx="256" cy="256" r="${r}" fill="${BG}" stroke="${LINE}" stroke-width="14"/>
  <text x="256" y="${baseline}" text-anchor="middle"
    font-family="Helvetica Neue, Helvetica, Arial, sans-serif"
    font-weight="900" font-size="${fontSize}" fill="${FG}">FC</text>
</svg>
`;
}

function hasCommand(cmd) {
  try {
    execFileSync(cmd, ['--version'], { stdio: 'ignore' });
    return true;
  } catch {
    return false;
  }
}

function rasterize(svgPath, pngPath, size) {
  if (hasCommand('rsvg-convert')) {
    execFileSync('rsvg-convert', ['-w', String(size), '-h', String(size), svgPath, '-o', pngPath]);
  } else {
    execFileSync('sips', ['-s', 'format', 'png', '-z', String(size), String(size), svgPath, '--out', pngPath], { stdio: 'ignore' });
  }
}

const targets = [
  { file: 'icon-192.png', size: 192, scale: 1 },
  { file: 'icon-512.png', size: 512, scale: 1 },
  { file: 'icon-maskable-512.png', size: 512, scale: 0.75 },
  { file: 'apple-touch-icon.png', size: 180, scale: 1 },
];

const tmp = mkdtempSync(join(tmpdir(), 'futcarrara-icons-'));
try {
  for (const { file, size, scale } of targets) {
    const svgPath = join(tmp, `${file}.svg`);
    writeFileSync(svgPath, iconSvg(scale));
    rasterize(svgPath, join('public', file), size);
    console.log(`public/${file} (${size}x${size})`);
  }
} finally {
  rmSync(tmp, { recursive: true, force: true });
}
```

- [ ] **Step 2: Rodar o script**

Run: `node scripts/generate-icons.mjs`
Expected: imprime as 4 linhas `public/...png` e os arquivos existem.

- [ ] **Step 3: Verificar dimensões**

Run: `sips -g pixelWidth -g pixelHeight public/icon-192.png public/icon-512.png public/icon-maskable-512.png public/apple-touch-icon.png`
Expected: 192×192, 512×512, 512×512, 180×180.

Abrir os PNGs (Read tool ou preview) e confirmar visualmente: fundo verde escuro, círculo central, "FC" legível; no maskable, o conteúdo menor (dentro da safe zone).

- [ ] **Step 4: Commit**

```bash
git add scripts/generate-icons.mjs public/icon-192.png public/icon-512.png public/icon-maskable-512.png public/apple-touch-icon.png
git commit -m "PWA: ícones gerados (192/512/maskable/apple-touch)"
```

---

### Task 2: Manifest + metadata no layout

**Files:**
- Create: `public/manifest.json`
- Modify: `src/app/layout.tsx:19-26` (bloco `metadata`)

**Interfaces:**
- Consumes: PNGs da Task 1 (`/icon-192.png`, `/icon-512.png`, `/icon-maskable-512.png`, `/apple-touch-icon.png`).
- Produces: `/manifest.json` servido da raiz — o precache da Task 4 o inclui automaticamente (varre `out/`).

- [ ] **Step 1: Escrever `public/manifest.json`**

```json
{
  "name": "Fut Carrara",
  "short_name": "Fut Carrara",
  "description": "Marque quem chegou e tire times equilibrados em segundos.",
  "lang": "pt-BR",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "background_color": "#0b1a12",
  "theme_color": "#0b1a12",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
    { "src": "/icon-maskable-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

- [ ] **Step 2: Atualizar `metadata` em `src/app/layout.tsx`**

Substituir o bloco atual:

```tsx
export const metadata: Metadata = {
  title: 'Fut Carrara',
  description: 'Marque quem chegou e tire times equilibrados em segundos.',
};
```

por:

```tsx
export const metadata: Metadata = {
  title: 'Fut Carrara',
  description: 'Marque quem chegou e tire times equilibrados em segundos.',
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'Fut Carrara',
  },
  icons: {
    apple: '/apple-touch-icon.png',
  },
};
```

Não mexer no `viewport` (o `themeColor` já está lá).

- [ ] **Step 3: Build e verificar o HTML gerado**

Run: `npm run build && grep -o 'rel="manifest"[^>]*' out/index.html && grep -o 'apple-touch-icon[^>]*' out/index.html`
Expected: build verde; saída contém `rel="manifest" href="/manifest.json"` e referência a `apple-touch-icon`. `out/manifest.json` existe (copiado de `public/`).

- [ ] **Step 4: Commit**

```bash
git add public/manifest.json src/app/layout.tsx
git commit -m "PWA: manifest e metadata de instalação (iOS incluso)"
```

---

### Task 3: Funções puras do gerador de SW (TDD)

**Files:**
- Create: `scripts/generate-sw.mjs` (só as funções puras nesta task; o `main` entra na Task 4)
- Test: `scripts/generate-sw.test.mjs`

**Interfaces:**
- Consumes: nada.
- Produces: exports nomeados usados pela Task 4 e pelo teste:
  - `routeForFile(relPath: string): string` — caminho relativo dentro de `out/` → URL de precache. `index.html` → `/`; `elenco.html` → `/elenco`; demais arquivos → `/` + caminho literal (posix).
  - `shouldPrecache(relPath: string): boolean` — `false` para `404.html`, `sw.js` e dotfiles (qualquer segmento começando com `.`); `true` para o resto.
  - `buildPrecacheList(relPaths: string[]): string[]` — filtra com `shouldPrecache`, mapeia com `routeForFile`, dedup e ordena.

- [ ] **Step 1: Escrever o teste que falha — `scripts/generate-sw.test.mjs`**

```js
import { describe, expect, it } from 'vitest';
import { buildPrecacheList, routeForFile, shouldPrecache } from './generate-sw.mjs';

describe('routeForFile', () => {
  it('mapeia index.html para a raiz', () => {
    expect(routeForFile('index.html')).toBe('/');
  });

  it('mapeia páginas html para a rota sem extensão', () => {
    expect(routeForFile('elenco.html')).toBe('/elenco');
    expect(routeForFile('sorteio.html')).toBe('/sorteio');
  });

  it('mantém arquivos não-html como caminho literal', () => {
    expect(routeForFile('elenco.txt')).toBe('/elenco.txt');
    expect(routeForFile('manifest.json')).toBe('/manifest.json');
    expect(routeForFile('_next/static/chunks/abc.js')).toBe('/_next/static/chunks/abc.js');
  });
});

describe('shouldPrecache', () => {
  it('exclui 404.html, sw.js e dotfiles', () => {
    expect(shouldPrecache('404.html')).toBe(false);
    expect(shouldPrecache('sw.js')).toBe(false);
    expect(shouldPrecache('.DS_Store')).toBe(false);
    expect(shouldPrecache('_next/static/.hidden')).toBe(false);
  });

  it('inclui html, txt, assets e manifest', () => {
    expect(shouldPrecache('index.html')).toBe(true);
    expect(shouldPrecache('index.txt')).toBe(true);
    expect(shouldPrecache('_next/static/css/app.css')).toBe(true);
    expect(shouldPrecache('manifest.json')).toBe(true);
    expect(shouldPrecache('icon-192.png')).toBe(true);
  });
});

describe('buildPrecacheList', () => {
  it('filtra, mapeia, deduplica e ordena', () => {
    const files = ['elenco.html', '404.html', 'index.html', 'elenco.html', '.DS_Store', 'elenco.txt'];
    expect(buildPrecacheList(files)).toEqual(['/', '/elenco', '/elenco.txt']);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test:run -- scripts/generate-sw.test.mjs`
Expected: FAIL — `Failed to load .../generate-sw.mjs` (arquivo não existe).

- [ ] **Step 3: Implementar as funções puras — `scripts/generate-sw.mjs`**

```js
// Gera out/sw.js após o build: varre out/, monta o precache e injeta no template.
// Roda via "postbuild" (ver Task 4). Funções puras exportadas para teste.
import { sep } from 'node:path';

export function routeForFile(relPath) {
  const posix = relPath.split(sep).join('/');
  if (!posix.endsWith('.html')) return `/${posix}`;
  if (posix === 'index.html') return '/';
  return `/${posix.slice(0, -'.html'.length)}`;
}

export function shouldPrecache(relPath) {
  const posix = relPath.split(sep).join('/');
  if (posix === '404.html' || posix === 'sw.js') return false;
  return posix.split('/').every((segment) => !segment.startsWith('.'));
}

export function buildPrecacheList(relPaths) {
  const routes = relPaths.filter(shouldPrecache).map(routeForFile);
  return [...new Set(routes)].sort();
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test:run -- scripts/generate-sw.test.mjs`
Expected: PASS (6 testes).

- [ ] **Step 5: Commit**

```bash
git add scripts/generate-sw.mjs scripts/generate-sw.test.mjs
git commit -m "PWA: funções puras do gerador de service worker (TDD)"
```

---

### Task 4: Template do SW + postbuild

**Files:**
- Create: `scripts/sw-template.js`
- Modify: `scripts/generate-sw.mjs` (adicionar hash de versão + `main`)
- Modify: `package.json` (script `postbuild`)

**Interfaces:**
- Consumes: `buildPrecacheList` da Task 3; template com placeholders `__PRECACHE__` e `__VERSION__`.
- Produces: `out/sw.js` completo após `npm run build`; a Task 5 registra `/sw.js`.

- [ ] **Step 1: Escrever `scripts/sw-template.js`**

```js
/* Service worker do Fut Carrara. Gerado por scripts/generate-sw.mjs — não editar out/sw.js à mão. */
const VERSION = '__VERSION__';
const CACHE_NAME = `futcarrara-${VERSION}`;
const PRECACHE = __PRECACHE__;

self.addEventListener('install', (event) => {
  // Sem skipWaiting: a versão nova só assume quando o app fechar (atualização passiva).
  event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.addAll(PRECACHE)));
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches
      .keys()
      .then((keys) =>
        Promise.all(
          keys
            .filter((key) => key.startsWith('futcarrara-') && key !== CACHE_NAME)
            .map((key) => caches.delete(key)),
        ),
      )
      .then(() => self.clients.claim()),
  );
});

self.addEventListener('fetch', (event) => {
  const { request } = event;
  if (request.method !== 'GET') return;
  if (new URL(request.url).origin !== self.location.origin) return;

  // Cache-first: o conteúdo é imutável dentro de uma versão.
  // ignoreSearch cobre os payloads RSC (/rota.txt?_rsc=...).
  event.respondWith(
    caches.match(request, { ignoreSearch: true }).then((cached) => {
      if (cached) return cached;
      return fetch(request).catch(() => {
        if (request.mode === 'navigate') {
          return caches.match('/').then((home) => home ?? Response.error());
        }
        return Response.error();
      });
    }),
  );
});
```

- [ ] **Step 2: Completar `scripts/generate-sw.mjs` com hash e `main`**

Adicionar aos imports e ao final do arquivo (mantendo as funções puras da Task 3 intactas):

```js
import { createHash } from 'node:crypto';
import { readFileSync, readdirSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { fileURLToPath } from 'node:url';

export function collectFiles(dir, base = dir) {
  return readdirSync(dir, { withFileTypes: true }).flatMap((entry) => {
    const full = join(dir, entry.name);
    if (entry.isDirectory()) return collectFiles(full, base);
    return [full.slice(base.length + 1)];
  });
}

function hashFiles(outDir, relPaths) {
  const hash = createHash('sha256');
  for (const relPath of [...relPaths].sort()) {
    hash.update(relPath);
    hash.update(readFileSync(join(outDir, relPath)));
  }
  return hash.digest('hex').slice(0, 16);
}

function main() {
  const outDir = 'out';
  const files = collectFiles(outDir).filter(shouldPrecache);
  const precache = buildPrecacheList(files);
  const version = hashFiles(outDir, files);
  const template = readFileSync('scripts/sw-template.js', 'utf8');
  const sw = template
    .replace('__VERSION__', version)
    .replace('__PRECACHE__', JSON.stringify(precache, null, 2));
  writeFileSync(join(outDir, 'sw.js'), sw);
  console.log(`out/sw.js: versão ${version}, ${precache.length} URLs no precache`);
}

if (process.argv[1] && fileURLToPath(import.meta.url) === process.argv[1]) {
  main();
}
```

- [ ] **Step 3: Adicionar `postbuild` ao `package.json`**

Em `"scripts"`, logo após `"build"`:

```json
"postbuild": "node scripts/generate-sw.mjs",
```

- [ ] **Step 4: Testes continuam verdes**

Run: `npm run test:run`
Expected: PASS — todos os testes do projeto, incluindo os da Task 3 (o `main` não roda ao importar).

- [ ] **Step 5: Build e inspecionar `out/sw.js`**

Run: `npm run build && node -e "new Function(require('fs').readFileSync('out/sw.js','utf8')); console.log('sw.js parses')" && grep -c '"/' out/sw.js`
Expected: log `out/sw.js: versão <hash>, N URLs no precache` (N > 10); `sw.js parses`; o precache contém `"/"`, `"/elenco"`, `"/sorteio"`, `"/resultado"`, `"/manifest.json"`, `"/elenco.txt"` e chunks de `_next/static`. Conferir com: `grep -E '"/(elenco|sorteio|resultado|manifest)' out/sw.js`.

- [ ] **Step 6: Commit**

```bash
git add scripts/sw-template.js scripts/generate-sw.mjs package.json
git commit -m "PWA: service worker gerado no postbuild com precache completo"
```

---

### Task 5: Registro do SW + verificação end-to-end

**Files:**
- Create: `src/components/ui/ServiceWorkerRegister.tsx`
- Modify: `src/app/layout.tsx:33-40` (montar o componente)

**Interfaces:**
- Consumes: `out/sw.js` (Task 4) servido em `/sw.js`.
- Produces: SW registrado em produção. Nada exportado para outras tasks.

- [ ] **Step 1: Escrever `src/components/ui/ServiceWorkerRegister.tsx`**

```tsx
'use client';

import { useEffect } from 'react';

export function ServiceWorkerRegister() {
  useEffect(() => {
    if (process.env.NODE_ENV !== 'production') return;
    if (!('serviceWorker' in navigator)) return;
    navigator.serviceWorker.register('/sw.js').catch(() => {
      // Sem SW o app segue funcionando online; nada a fazer.
    });
  }, []);

  return null;
}
```

- [ ] **Step 2: Montar no layout**

Em `src/app/layout.tsx`, adicionar o import:

```tsx
import { ServiceWorkerRegister } from '@/components/ui/ServiceWorkerRegister';
```

e no JSX do body:

```tsx
<body className="min-h-dvh pb-[calc(7rem+env(safe-area-inset-bottom))] font-body">
  {children}
  <MobileNav />
  <ServiceWorkerRegister />
</body>
```

- [ ] **Step 3: Lint, testes e build**

Run: `npm run lint && npm run test:run && npm run build`
Expected: tudo verde; postbuild regenera `out/sw.js`.

- [ ] **Step 4: Verificação offline manual (Chrome)**

```bash
python3 -m http.server 8080 -d out
```

No Chrome, abrir `http://localhost:8080`:
1. DevTools → Application → Manifest: sem erros/warnings, ícones aparecem, app instalável.
2. Application → Service workers: `sw.js` ativo (recarregar a página uma vez após o primeiro load).
3. Marcar **Offline** (Network) e navegar: `/`, `/elenco`, `/sorteio`, `/resultado` carregam do cache, inclusive navegação client-side.
4. Desmarcar Offline, parar o servidor.

Expected: todas as rotas funcionam offline. Se algo falhar, investigar antes de commitar (superpowers:systematic-debugging).

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/ServiceWorkerRegister.tsx src/app/layout.tsx
git commit -m "PWA: registro do service worker em produção"
```
